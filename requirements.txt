# Core UI
streamlit>=1.28.1

# RAG / vector store
chromadb==0.5.23           # Pinned: schema changes across minor versions break the DB
protobuf>=4.22.0,<5.0.0   # Pin required: chromadb 0.5.x has protobuf version conflicts on Streamlit Cloud

# LLM client
openai>=1.30.0             # Groq uses OpenAI-compatible API
anthropic>=0.40.0          # LLM-as-judge in evaluation.py (optional, set ANTHROPIC_API_KEY)

# HTTP / config
requests>=2.31.0           # Sync HTTP for embedding API health-check probe
python-dotenv>=1.0.0       # Loads .env into os.environ at startup

# Testing (not required for Streamlit Cloud deployment)
pytest>=7.0.0
pytest-asyncio>=0.21.0
